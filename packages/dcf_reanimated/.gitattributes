# Auto detect text files and perform LF normalization
* text=auto

# Git LFS configuration for Skia library
# Split into architecture-specific files (each under 2GB GitHub limit)
# Regular developers get these automatically - NO BUILDING NEEDED!
# Only framework contributors rebuild if they modify Skia
ios/Skia/lib/libskia_x86_64.a filter=lfs diff=lfs merge=lfs -text
ios/Skia/lib/libskia_arm64.a filter=lfs diff=lfs merge=lfs -text
ios/Skia/lib/libskia_arm64e.a filter=lfs diff=lfs merge=lfs -text

